{
  "servers": [
    {
      "name": "echo",
      "command": "node",
      "args": ["scripts/mcp-echo-server.mjs"],
      "disabled": false,
      "tools": [
        {
          "name": "echo",
          "description": "Echo back arguments (live stdio example)",
          "parameters": {
            "type": "object",
            "properties": {
              "message": { "type": "string" }
            },
            "required": ["message"]
          }
        }
      ]
    }
  ]
}
